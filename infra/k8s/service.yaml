apiVersion: v1
kind: Service
metadata:
  name: payment-gateway
  namespace: fintech
spec:
  type: LoadBalancer
  selector:
    app: payment-gateway
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
